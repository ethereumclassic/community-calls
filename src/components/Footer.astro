---
import { Icon } from "astro-icon/components";
import { siteConfig } from "../lib/config";
import Link from "./Link.astro";
import ETCLogo from "./ETCLogo.astro";
---

<footer
  class="relative border-t border-etc-green/10 bg-void/50 backdrop-blur-sm"
>
  <!-- Top accent line -->
  <div
    class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-etc-green/30 to-transparent"
  >
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="flex flex-col md:flex-row items-center justify-between gap-6">
      <!-- Left: Branding -->
      <div class="flex items-center gap-3 text-gray-500">
        <div class="relative">
          <ETCLogo color="text-gray-500" class="h-8 w-8 opacity-60" />
        </div>
        <div class="text-xs font-mono text-gray-500">
          <span>Ethereum Classic</span> Community Calls
        </div>
      </div>

      <!-- Center: Tagline -->
      <p
        class="text-xs font-mono text-gray-500 text-center order-last md:order-none"
      >
        Built with <code
          class="px-1.5 py-0.5 bg-etc-green/10 text-etc-green rounded text-[10px]"
          >&lt;3</code
        > for the Original Ethereum Vision
      </p>

      <!-- Right: Links -->
      <div class="flex items-center gap-6">
        <Link
          href={siteConfig.social.website}
          text="ethereumclassic.org"
          class="text-xs font-mono text-gray-500 hover:text-etc-green transition-colors duration-200"
        />
        <button
          type="button"
          onclick="document.getElementById('subscribe-modal-global').showModal()"
          class="social-link cursor-pointer"
          aria-label="Subscribe to Calendar"
        >
          <Icon name="lucide:calendar" class="icon-sm" />
        </button>
        <Link
          href={siteConfig.social.discord}
          class="social-link"
          aria-label="Discord"
        >
          <Icon name="simple-icons:discord" class="icon-sm" />
        </Link>
        <Link
          href={siteConfig.social.youtube}
          class="social-link"
          aria-label="YouTube"
        >
          <Icon name="simple-icons:youtube" class="icon-sm" />
        </Link>
        <Link
          href={siteConfig.social.github}
          class="social-link"
          aria-label="GitHub"
        >
          <Icon name="simple-icons:github" class="icon-sm" />
        </Link>
      </div>
    </div>
    <!-- Archival disclaimer -->
    <p class="mt-6 text-xs font-mono text-gray-500 text-center leading-relaxed">
    Community call pages are archival records of informal community discussions and are not authoritative sources of network decisions or consensus. Transcripts are the sole source of truth for what was said during the call; summaries, titles, and layouts are non-authoritative and may change.
    </p>
  </div>
</footer>
